# Jev diagnostic layer

Optional development-only diagnosis through TypeSafe AI Jev. Set `JEV_API_KEY` in the environment. Missing keys, network failures, timeouts, and invalid responses return no result so development continues normally.
